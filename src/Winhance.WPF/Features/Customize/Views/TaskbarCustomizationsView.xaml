<UserControl x:Class="Winhance.WPF.Features.Customize.Views.TaskbarCustomizationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Winhance.WPF.Features.Customize.Views"
             xmlns:viewmodels="clr-namespace:Winhance.WPF.Features.Customize.ViewModels"
             xmlns:controls="clr-namespace:Winhance.WPF.Features.Common.Controls"
             xmlns:behaviors="clr-namespace:Winhance.WPF.Features.Common.Behaviors"
             xmlns:enums="clr-namespace:Winhance.Core.Features.Common.Enums;assembly=Winhance.Core"
             d:DataContext="{d:DesignInstance Type=viewmodels:TaskbarCustomizationsViewModel}"
             mc:Ignorable="d">

    <StackPanel Margin="0,10,0,10">
        <!-- Clean Taskbar Button -->
        <Grid Margin="10,0,10,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <!-- Left: Title and Description -->
            <StackPanel VerticalAlignment="Center" Margin="15,0,0,0">
                <TextBlock Text="Clean Taskbar"
                           Foreground="{DynamicResource PrimaryTextColor}"
                           FontWeight="Bold"
                           FontSize="14"/>
                <TextBlock Text="Unpins all Items from the Taskbar"
                           Foreground="{DynamicResource PrimaryTextColor}"
                           FontSize="12"/>
            </StackPanel>
            
            <!-- Right: Button -->
            <Button Grid.Column="1"
                    Content="Execute"
                    Command="{Binding CleanTaskbarCommand}"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Margin="10,5,10,5"
                    Padding="10,5"
                    HorizontalAlignment="Right">
                <Button.ToolTip>
                    <ToolTip>
                        <TextBlock TextWrapping="Wrap" MaxWidth="300">
                            Cleans the Taskbar by removing all pinned items.
                            <LineBreak/>
                            Note: This will restart Explorer and may require a system restart to fully apply.
                        </TextBlock>
                    </ToolTip>
                </Button.ToolTip>
            </Button>
        </Grid>
        
        <!-- Separator -->
        <Separator Margin="25,0,25,20"/>
        
        <!-- Container for ComboBox Settings -->
        <ItemsControl x:Name="TaskbarComboBoxSettingsItemsControl"
                      ItemsSource="{Binding ComboBoxSettings}"
                      Margin="0,0,0,10">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="10,0,10,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- Left: Icon + Label -->
                        <StackPanel Grid.Column="0" 
                                   Orientation="Horizontal" 
                                   VerticalAlignment="Center" 
                                   Margin="15,5,0,5">
                            <!-- Icon based on setting name -->
                            <TextBlock FontFamily="{StaticResource MaterialSymbolsFont}" 
                                      FontSize="20"
                                      FontWeight="SemiBold" 
                                      Margin="0,0,8,0" 
                                      VerticalAlignment="Center" 
                                      Foreground="{DynamicResource PrimaryTextColor}">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Style.Triggers>
                                            <!-- Search icon for Search in Taskbar -->
                                            <DataTrigger Binding="{Binding FullName}" Value="Search in Taskbar">
                                                <Setter Property="Text" Value="&#xE8B6;"/>
                                            </DataTrigger>
                                            <!-- Alignment icon for Taskbar Alignment -->
                                            <DataTrigger Binding="{Binding FullName}" Value="Taskbar Alignment">
                                                <Setter Property="Text" Value="&#xF7E6;"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                            
                            <!-- Label Text -->
                            <TextBlock Text="{Binding FullName}"
                                      Foreground="{DynamicResource PrimaryTextColor}"
                                      FontSize="14"
                                      VerticalAlignment="Center">
                                <TextBlock.ToolTip>
                                    <ToolTip>
                                        <ContentPresenter Content="{Binding}"
                                                ContentTemplate="{StaticResource RegistrySettingTooltipTemplate}"/>
                                    </ToolTip>
                                </TextBlock.ToolTip>
                            </TextBlock>
                        </StackPanel>
                        
                        <!-- Right: ComboBox -->
                        <ComboBox Grid.Column="1"
                                  Width="150"
                                  Margin="0,5,15,5"
                                  HorizontalAlignment="Right"
                                  VerticalAlignment="Center"
                                  ItemsSource="{Binding ComboBoxOptions}"
                                  SelectedItem="{Binding SelectedValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        
        <!-- Separator between ComboBox and Toggle settings -->
        <Separator Margin="25,0,25,10" 
                   Visibility="{Binding HasComboBoxSettings, Converter={StaticResource BooleanToVisibilityConverter}}"/>

        <!-- Container for Toggle Settings -->
        <ItemsControl x:Name="TaskbarToggleSettingsItemsControl"
                      ItemsSource="{Binding ToggleSettings}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" HorizontalAlignment="Left"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemContainerStyle>
                <Style BasedOn="{StaticResource ResponsiveItemContainerStyle}" TargetType="FrameworkElement">
                    <Setter Property="Grid.ColumnSpan"
                            Value="{Binding IsGroupHeader, Converter={StaticResource BooleanToGridSpanConverter}}"/>
                    <Setter Property="Width" Value="0"/>
                    <Setter Property="Margin" Value="0,0,0,0"/>
                </Style>
            </ItemsControl.ItemContainerStyle>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <!-- Group header template -->
                        <TextBlock Text="{Binding Name}"
                                   Foreground="{DynamicResource PrimaryTextColor}"
                                   FontWeight="SemiBold"
                                   Margin="25,15,0,5"
                                   Visibility="{Binding IsGroupHeader, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                        <!-- Regular setting template -->
                        <StackPanel Orientation="Horizontal"
                                    Visibility="{Binding IsGroupHeader, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
                            <ToggleButton Style="{StaticResource MaterialToggleSwitch}"
                                          IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                          Margin="15,5,0,5"
                                          HorizontalAlignment="Left"
                                          VerticalAlignment="Center"
                                          Content="">
                                <ToggleButton.ToolTip>
                                    <ToolTip>
                                        <ContentPresenter Content="{Binding}"
                                                ContentTemplate="{StaticResource RegistrySettingTooltipTemplate}"/>
                                    </ToolTip>
                                </ToggleButton.ToolTip>
                            </ToggleButton>
                            <TextBlock Text="{Binding FullName}"
                                       Foreground="{DynamicResource PrimaryTextColor}"
                                       Margin="0,5,0,5"
                                       FontSize="14"
                                       VerticalAlignment="Center">
                                <TextBlock.ToolTip>
                                    <ToolTip>
                                        <ContentPresenter Content="{Binding}"
                                                ContentTemplate="{StaticResource RegistrySettingTooltipTemplate}"/>
                                    </ToolTip>
                                </TextBlock.ToolTip>
                            </TextBlock>
                            <!-- Warning Icon (visible only when registry value is null) -->
                            <TextBlock Text="&#xEA76;"
                                       FontFamily="{StaticResource MaterialSymbolsFont}"
                                       FontSize="18"
                                       Foreground="#FF2800"
                                       Margin="5,5,0,0"
                                       VerticalAlignment="Center"
                                       Visibility="{Binding IsRegistryValueNull, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <TextBlock.ToolTip>
                                    <ToolTip>
                                        <TextBlock TextWrapping="Wrap"
                                                MaxWidth="300">
                                            Registry key or value doesn't exist and Windows is using default settings.<LineBreak/>
                                            Recommendation: Toggle setting to desired state to set explicitly or ignore if you want to continue using Windows default settings.
                                        </TextBlock>
                                    </ToolTip>
                                </TextBlock.ToolTip>
                            </TextBlock>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</UserControl>
