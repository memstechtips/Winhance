<UserControl x:Class="Winhance.WPF.Features.Customize.Views.TaskbarCustomizationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:Winhance.WPF.Features.Customize.ViewModels"
             xmlns:controls="clr-namespace:Winhance.WPF.Features.Common.Controls"
             xmlns:behaviors="clr-namespace:Winhance.WPF.Features.Common.Behaviors"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             d:DataContext="{d:DesignInstance Type=viewModels:TaskbarCustomizationsViewModel}"
             mc:Ignorable="d"
             Visibility="{Binding HasVisibleSettings, Converter={StaticResource BooleanToVisibilityConverter}}">


    <StackPanel>
        <!-- Collapsible Header -->
        <Border x:Name="HeaderBorder"
                Background="{DynamicResource ContentSectionBorderBrush}"
                CornerRadius="5"
                Margin="0,5,0,5"
                Effect="{StaticResource ShadowEffect}"
                Cursor="Hand"
                ClipToBounds="False"
                Panel.ZIndex="100">
            <Border.InputBindings>
                <MouseBinding MouseAction="LeftClick" Command="{Binding ToggleExpandCommand}"/>
            </Border.InputBindings>
            <Grid x:Name="Header" Height="40">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- Left content -->
                <StackPanel Orientation="Horizontal" 
                           VerticalAlignment="Center"
                           HorizontalAlignment="Left"
                           Grid.Column="0">
                    <!-- Category Icon -->
                    <materialDesign:PackIcon Kind="DockBottom"
                                         Width="24"
                                         Height="24"
                                         Margin="15,6,5,6"
                                         Foreground="{DynamicResource PrimaryTextColor}"
                                         VerticalAlignment="Center"/>
                    <!-- Category Name -->
                    <TextBlock Text="{Binding DisplayName}"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Margin="5,6,0,6"
                               Foreground="{DynamicResource PrimaryTextColor}"
                               VerticalAlignment="Center"/>
                </StackPanel>
                
                <!-- Chevron - positioned in right column with fixed alignment -->
                <materialDesign:PackIcon x:Name="HeaderIcon"
                                         Kind="ChevronDown"
                                         Width="24"
                                         Height="24"
                                         VerticalAlignment="Center"
                                         HorizontalAlignment="Right"
                                         Foreground="{DynamicResource PrimaryTextColor}"
                                         Margin="0,0,15,0"
                                         Grid.Column="1"
                                         RenderTransformOrigin="0.5,0.5">
                    <materialDesign:PackIcon.RenderTransform>
                        <RotateTransform Angle="{Binding IsExpanded, Converter={StaticResource BooleanToRotationConverter}}"/>
                    </materialDesign:PackIcon.RenderTransform>
                </materialDesign:PackIcon>
            </Grid>
        </Border>
        
        <!-- Content Section -->
        <Border Background="{DynamicResource ContentSectionBorderBrush}"
                CornerRadius="5"
                Margin="0,0,0,5"
                Effect="{StaticResource LightShadowEffect}"
                Visibility="{Binding IsExpanded, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel Margin="0,10,0,10">
                <!-- Clean Taskbar Button -->
                <Grid Margin="10,0,10,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- Left: Title and Description -->
                    <StackPanel VerticalAlignment="Center" Margin="15,0,0,0">
                        <TextBlock Text="Clean Taskbar"
                                   Foreground="{DynamicResource PrimaryTextColor}"
                                   FontWeight="Bold"
                                   FontSize="14"/>
                        <TextBlock Text="Unpins all Items except File Explorer"
                                   Foreground="{DynamicResource PrimaryTextColor}"
                                   FontSize="12"/>
                    </StackPanel>
                    
                    <!-- Right: Button -->
                    <Button Grid.Column="1"
                            Content="Execute"
                            Command="{Binding CleanTaskbarCommand}"
                            Style="{StaticResource PrimaryButtonStyle}"
                            Margin="10,5,10,5"
                            Padding="10,5"
                            HorizontalAlignment="Right">
                        <Button.ToolTip>
                            <ToolTip>
                                <TextBlock TextWrapping="Wrap" MaxWidth="300">
                                    Cleans the Taskbar by removing all pinned apps except File Explorer.
                                    <LineBreak/>
                                    This will:
                                    <LineBreak/>
                                    - Delete the Taskband registry key
                                    <LineBreak/>
                                    - Recreate the Quick Launch directories
                                    <LineBreak/>
                                    - Pin only File Explorer to the taskbar
                                    <LineBreak/>
                                    Note: This will restart Explorer and may require a system restart to fully apply.
                                </TextBlock>
                            </ToolTip>
                        </Button.ToolTip>
                    </Button>
                </Grid>

                <!-- Container for Setting Labels & Toggles -->
                <ItemsControl x:Name="TaskbarCustomizationsItemsControl"
                              ItemsSource="{Binding Settings}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Vertical"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemContainerStyle>
                        <Style BasedOn="{StaticResource SettingsItemContainerStyle}" TargetType="ContentPresenter">
                        </Style>
                    </ItemsControl.ItemContainerStyle>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <!-- Group header template -->
                            <Grid>
                                <TextBlock Text="{Binding Name}"
                                           Foreground="{DynamicResource PrimaryTextColor}"
                                           FontWeight="SemiBold"
                                           Margin="25,15,0,5"
                                           Visibility="{Binding IsGroupHeader, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                                <!-- Setting Labels & Controls -->
                                <Grid Visibility="{Binding IsVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <!-- Setting Text and Description (Left-aligned) -->
                                    <StackPanel Grid.Column="0" 
                                                HorizontalAlignment="Left" 
                                                VerticalAlignment="Center"
                                                Margin="15,10,0,0">
                                        <TextBlock Text="{Binding Name}"
                                                   FontWeight="SemiBold"
                                                   Style="{StaticResource AppItemTextStyle}"
                                                   VerticalAlignment="Center"/>
                                        <TextBlock Text="{Binding Description}"
                                                   FontSize="12"
                                                   Foreground="{DynamicResource SecondaryTextColor}"
                                                   TextWrapping="Wrap"
                                                   Margin="0,2,0,0"
                                                   VerticalAlignment="Center"/>
                                    </StackPanel>
                                    
                                    <!-- ComboBox for ComboBox settings (Right-aligned) -->
                                    <ComboBox Grid.Column="1"
                                              ItemsSource="{Binding ComboBoxOptions}"
                                              SelectedItem="{Binding SelectedValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                              Width="150"
                                              Margin="15,13,15,3"
                                              HorizontalAlignment="Right"
                                              VerticalAlignment="Center"
                                              Visibility="{Binding ControlType, Converter={StaticResource ComboBoxTypeToVisibilityConverter}}"
                                              Panel.ZIndex="10">
                                        <ComboBox.ToolTip>
                                            <ToolTip>
                                                <ContentPresenter Content="{Binding}"
                                                        ContentTemplate="{StaticResource CombinedSettingTooltipTemplate}"/>
                                            </ToolTip>
                                        </ComboBox.ToolTip>
                                    </ComboBox>
                                    
                                    <!-- Toggle for binary settings -->
                                    <ToggleButton Grid.Column="1"
                                                  Style="{StaticResource MaterialToggleSwitch}"
                                                  IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                  Margin="15,3,15,3"
                                                  HorizontalAlignment="Right"
                                                  VerticalAlignment="Center"
                                                  Visibility="{Binding ControlType, Converter={StaticResource BinaryToggleTypeToVisibilityConverter}}"
                                                  Content="">
                                        <ToggleButton.ToolTip>
                                            <ToolTip>
                                                <ContentPresenter Content="{Binding}"
                                                        ContentTemplate="{StaticResource CombinedSettingTooltipTemplate}"/>
                                            </ToolTip>
                                        </ToggleButton.ToolTip>
                                    </ToggleButton>
                                    
                                    <!-- Warning Icon (visible only when registry value is null) -->
                                    <TextBlock Grid.Column="1"
                                               Text="&#xEA76;"
                                               FontFamily="{StaticResource MaterialSymbolsFont}"
                                               FontSize="18"
                                               Foreground="#FF2800"
                                               Margin="5,5,15,0"
                                               HorizontalAlignment="Right"
                                               VerticalAlignment="Center"
                                               Visibility="{Binding IsRegistryValueNull, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <TextBlock.ToolTip>
                                            <ToolTip>
                                                <TextBlock TextWrapping="Wrap"
                                                        MaxWidth="300">
                                                    Registry key or value doesn't exist and Windows is using default settings.<LineBreak/>
                                                    Recommendation: Toggle setting to desired state to set explicitly or ignore if you want to continue using Windows default settings.
                                                </TextBlock>
                                            </ToolTip>
                                        </TextBlock.ToolTip>
                                    </TextBlock>
                                </Grid>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>