<UserControl x:Class="Winhance.WPF.Features.SoftwareApps.Views.WindowsAppsTableView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="clr-namespace:Winhance.WPF.Features.SoftwareApps.ViewModels"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:local="clr-namespace:Winhance.WPF.Features.Common.Controls"
    xmlns:controls="clr-namespace:Winhance.WPF.Features.Common.Controls"
    xmlns:behaviors="clr-namespace:Winhance.WPF.Features.Common.Behaviors" 
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    d:DataContext="{d:DesignInstance Type=viewModels:WindowsAppsViewModel}" mc:Ignorable="d">

    <controls:ResponsiveScrollViewer>
        <StackPanel Margin="10">

            <!-- Combined Table View for All Items -->
            <ListView x:Name="AppsListView" ItemsSource="{Binding AllItemsView}" Style="{DynamicResource ThemedListView}" ItemContainerStyle="{DynamicResource ThemedListViewItem}" Margin="10,0,10,10" behaviors:GridViewColumnResizeBehavior.Enabled="True" behaviors:GridViewColumnResizeBehavior.Stretch="True" behaviors:GridViewColumnResizeBehavior.StretchColumnIndex="1" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListView.View>
                    <GridView AllowsColumnReorder="False">
                        <GridView.ColumnHeaderContainerStyle>
                            <StaticResource ResourceKey="StretchedColumnGridViewHeader"/>
                        </GridView.ColumnHeaderContainerStyle>

                        <!-- Select All Column -->
                        <GridViewColumn Width="40">
                            <GridViewColumn.HeaderContainerStyle>
                                <StaticResource ResourceKey="FirstColumnHeader"/>
                            </GridViewColumn.HeaderContainerStyle>
                            <GridViewColumn.Header>
                                <CheckBox Style="{StaticResource CustomCheckBox}" IsChecked="{Binding IsAllSelected}" VerticalAlignment="Center"/>
                            </GridViewColumn.Header>
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox Style="{StaticResource CustomCheckBox}" IsChecked="{Binding IsSelected}" VerticalAlignment="Center"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <!-- Name Column -->
                        <GridViewColumn Width="Auto" HeaderContainerStyle="{StaticResource ThemedGridViewColumnHeader}">
                            <GridViewColumn.Header>
                                <GridViewColumnHeader Tag="Name" Content="Name">
                                    <GridViewColumnHeader.InputBindings>
                                        <MouseBinding MouseAction="LeftClick" Command="{Binding SortByCommand}" CommandParameter="Name"/>
                                    </GridViewColumnHeader.InputBindings>
                                </GridViewColumnHeader>
                            </GridViewColumn.Header>
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Name}" FontSize="18">
                                        <TextBlock.ToolTip>
                                            <ToolTip Content="{Binding Description}"/>
                                        </TextBlock.ToolTip>
                                    </TextBlock>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <!-- Type Column -->
                        <GridViewColumn Width="220" HeaderContainerStyle="{StaticResource ThemedGridViewColumnHeader}">
                            <GridViewColumn.Header>
                                <GridViewColumnHeader Tag="ItemType" Content="Type">
                                    <GridViewColumnHeader.InputBindings>
                                        <MouseBinding MouseAction="LeftClick" Command="{Binding SortByCommand}" CommandParameter="ItemType"/>
                                    </GridViewColumnHeader.InputBindings>
                                </GridViewColumnHeader>
                            </GridViewColumn.Header>
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding ItemType}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <!-- Status Column -->
                        <GridViewColumn Width="220" HeaderContainerStyle="{StaticResource ThemedGridViewColumnHeader}">
                            <GridViewColumn.Header>
                                <GridViewColumnHeader Tag="IsInstalled" Content="Status">
                                    <GridViewColumnHeader.InputBindings>
                                        <MouseBinding MouseAction="LeftClick" Command="{Binding SortByCommand}" CommandParameter="IsInstalled"/>
                                    </GridViewColumnHeader.InputBindings>
                                </GridViewColumnHeader>
                            </GridViewColumn.Header>
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Ellipse Style="{StaticResource AppStatusIndicatorStyle}"
                                                 Fill="{Binding IsInstalled, Converter={StaticResource InstalledStatusToColorConverter}}">
                                            <Ellipse.ToolTip>
                                                <ToolTip Content="{Binding IsInstalled, Converter={StaticResource InstalledStatusToTextConverter}}"/>
                                            </Ellipse.ToolTip>
                                        </Ellipse>
                                        <TextBlock Text="{Binding IsInstalled, Converter={StaticResource InstalledStatusToTextConverter}}" Margin="5,0,0,0" />
                                    </StackPanel>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>

                        <!-- Installable Column -->
                        <GridViewColumn Width="220">
                            <GridViewColumn.HeaderContainerStyle>
                                <StaticResource ResourceKey="LastColumnHeader"/>
                            </GridViewColumn.HeaderContainerStyle>
                            <GridViewColumn.Header>
                                <GridViewColumnHeader Tag="CanBeReinstalled" Content="Installable">
                                    <GridViewColumnHeader.InputBindings>
                                        <MouseBinding MouseAction="LeftClick" Command="{Binding SortByCommand}" CommandParameter="CanBeReinstalled"/>
                                    </GridViewColumnHeader.InputBindings>
                                </GridViewColumnHeader>
                            </GridViewColumn.Header>
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon
                                            Style="{StaticResource ReinstallabilityIndicatorStyle}"
                                            Kind="{Binding CanBeReinstalled, Converter={StaticResource BooleanToReinstallableIconConverter}}"
                                            Foreground="{Binding CanBeReinstalled, Converter={StaticResource StatusToColorConverter}}">
                                            <materialDesign:PackIcon.ToolTip>
                                                <ToolTip Content="{Binding CanBeReinstalled, Converter={StaticResource BooleanToReinstallableTextConverter}}"/>
                                            </materialDesign:PackIcon.ToolTip>
                                        </materialDesign:PackIcon>
                                        <TextBlock Text="{Binding CanBeReinstalled, Converter={StaticResource BooleanToReinstallableTextConverter}}" Margin="5,0,0,0" />
                                    </StackPanel>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>


                    </GridView>
                </ListView.View>
            </ListView>
        </StackPanel>
    </controls:ResponsiveScrollViewer>
</UserControl>
