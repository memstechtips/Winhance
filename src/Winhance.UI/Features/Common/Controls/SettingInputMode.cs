namespace Winhance.UI.Features.Common.Controls;

// The second axis of the settings card: (control type) x (input mode). Before the shared controls that matrix
// was ten hand-written DataTemplates - which is how the outcome overlay ended up on two of them and missing from the rest.
public enum SettingInputMode
{
    Single,

    Ac,

    Dc,
}
